# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info/
src/*.egg-info/

# Virtual environments
.venv

# Experiment artifacts (large / generated)
inputs/
outputs/
*.safetensors
*.parquet
*.npz

# Review / paper decks — local only, do not push to GitHub
*.pptx

# Hand-label sheets (user-specific; agreement results go in docs, not raw labels)
describe_label_sheet*.csv

# OS / editor
.DS_Store
.idea/
.vscode/

# Per-project Claude Code local state (scheduled tasks, etc.)
.claude/